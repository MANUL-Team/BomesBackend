# Забираем шаблоны
include:
  - '/gitlab-ci/template.yml'

# Ставим глобальную переменную проекта
# В шаблонах используется для именования Docker-образа
variables:
  PROJECT: "BomesNotificationService"
  DOCKERNAME: "bomes-notifications"
  IP: "172.20.1.80"

# Наследуемся, где надо — переопределяем переменные
# *В рамках примера это не нужно
build-notifications-service:
  extends: .build-template-servises

production-notifications-service:
  extends: .production-template-servises